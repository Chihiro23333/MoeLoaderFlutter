import 'dart:io';
import 'package:flutter_test/flutter_test.dart';
import 'package:to_json/yaml_rule_factory.dart';
import 'package:path/path.dart' as path;
import 'package:yaml/yaml.dart';

void main() {
  test('adds one to input values', () async{
    // print("path=${path.join(path.current ,"rules")}");
    // String html = await File(path.join(path.current ,"rules") + "/yande_detail.html").readAsString();
    // await YamlRuleFactory().init(Directory(path.join(path.current ,"rules")), Directory(path.join(path.current ,"images")));
    // String sourceName = "yande_common";
    // String pageName = "detailPage";
    // YamlMap doc = await YamlRuleFactory().create(sourceName);
    // var json = await ParserFactory().createParser().parseUseYaml(html, doc, pageName);
    // print("json=$json");
    //
    // var options = await ParserFactory().createParser().options(doc, pageName);
    // print("options=$options");
  });
}
